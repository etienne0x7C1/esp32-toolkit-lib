/**
 * minimal version of firmware: no deps required
 * just using basic firmware module
 * 
 * required build env: esp32doit-devkit-v1
**/

#include <test-module.h>

TestModule *testModule = new TestModule();